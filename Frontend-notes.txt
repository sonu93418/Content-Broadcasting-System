Frontend-Notes: Content Broadcasting System (ContentCast)
=========================================================

1. PROJECT STRUCTURE
-------------------
src/
├── app/                          # Next.js App Router pages
│   ├── layout.js                 # Root layout (AuthProvider, fonts, metadata)
│   ├── page.js                   # Landing page with auto-redirect
│   ├── login/page.js             # Authentication page
│   ├── teacher/
│   │   ├── dashboard/page.js     # Teacher dashboard with stats
│   │   ├── upload/page.js        # Content upload form
│   │   └── my-content/page.js    # Teacher's content listing
│   ├── principal/
│   │   ├── dashboard/page.js     # Principal dashboard with stats
│   │   ├── pending/page.js       # Pending approvals with actions
│   │   └── all-content/page.js   # All content with filters
│   └── live/[teacherId]/page.js  # Public live broadcasting page
├── components/
│   ├── layout/
│   │   ├── Sidebar.js            # Responsive sidebar with role-based nav
│   │   ├── DashboardLayout.js    # Layout wrapper for authenticated pages
│   │   └── ProtectedRoute.js     # Auth guard with role enforcement
│   └── ui/
│       ├── ContentCard.js        # Rich content display card
│       ├── EmptyState.js         # Empty data state
│       ├── ErrorState.js         # Error state with retry
│       ├── FileUpload.js         # Drag-and-drop file upload
│       ├── Modal.js              # Animated modal dialog
│       ├── SearchFilter.js       # Search and status filter bar
│       ├── SkeletonLoader.js     # Skeleton loading states
│       ├── StatCard.js           # Dashboard stat card
│       └── StatusBadge.js        # Status indicator badge
├── context/
│   └── AuthContext.js            # Auth state management
├── hooks/
│   ├── useContent.js             # Content data fetching hooks
│   └── useUpload.js              # File upload management hook
├── services/
│   ├── api-client.js             # HTTP client with token management
│   ├── auth.service.js           # Auth API service layer
│   ├── content.service.js        # Content API service layer
│   ├── approval.service.js       # Approval API service layer
│   └── mock-data.js              # Mock backend with simulated delays
└── utils/
    ├── constants.js              # App constants (roles, statuses, routes)
    └── helpers.js                # Utility functions


2. AUTHENTICATION FLOW
----------------------
- User submits email/password on /login
- AuthContext.login() calls authService.login()
- On success, JWT token stored in localStorage
- User object stored in AuthContext state
- Automatic redirect based on role (teacher → /teacher/dashboard, principal → /principal/dashboard)
- On page load, AuthContext checks for existing token and validates via getProfile()
- Token attached to all API calls via api-client.js getAuthHeaders()
- Logout clears token and redirects to /login
- ProtectedRoute component guards authenticated pages and enforces role access

Demo Credentials:
  Teacher:   teacher@school.com / teacher123
  Principal: principal@school.com / principal123


3. ROLE-BASED ROUTING
---------------------
- ProtectedRoute component wraps all dashboard pages
- Accepts `allowedRole` prop to restrict access
- Unauthenticated users → redirect to /login
- Wrong role → redirect to correct dashboard
- Sidebar navigation dynamically renders based on user.role
- /live/:teacherId is public, no auth required

Routes:
  Teacher:   /teacher/dashboard, /teacher/upload, /teacher/my-content
  Principal: /principal/dashboard, /principal/pending, /principal/all-content
  Public:    /live/:teacherId


4. API INTEGRATION APPROACH
---------------------------
- All API calls go through dedicated service files (auth.service.js, content.service.js, approval.service.js)
- NO direct API calls inside components
- Services currently use mock-data.js which simulates network delays (400-1000ms)
- To connect to real backend: replace mock imports in service files with apiCall() from api-client.js
- api-client.js provides: token management, auth headers, generic fetch wrapper
- Each service method has a comment showing the real API call equivalent

Replacing mock with real API:
  1. Update API_BASE_URL in constants.js
  2. In each service file, uncomment apiCall imports
  3. Replace mock function calls with apiCall() calls
  4. Adjust request/response shapes as needed


5. STATE MANAGEMENT APPROACH
-----------------------------
- AuthContext (React Context) for global auth state
- Custom hooks for data fetching (useContent, useTeacherContent, useContentStats, useLiveContent)
- Local component state for UI concerns (modals, filters, form state)
- React Hook Form + Zod for form state and validation
- No unnecessary global state - data fetched where needed
- Memoization via React.memo on UI components to prevent re-renders


6. ASSUMPTIONS
--------------
- JWT token stored in localStorage (secure httpOnly cookies preferred in production)
- File uploads create blob URLs for preview (real backend would return CDN URLs)
- Mock data simulates 600-1000ms network delays for realistic UX
- Live page polls every 30 seconds for new content
- Content rotation uses setTimeout based on rotationDuration field
- Schedule status (active/scheduled/expired) computed client-side from timestamps
- Max file size: 10MB, allowed types: JPG, PNG, GIF
- Error simulation disabled by default (set SIMULATE_ERRORS in mock-data.js)


7. RECENT ENHANCEMENTS (v2.0 - May 6, 2026)
--------------------------------------------

LOGIN PAGE ENHANCEMENT:
✅ Added NewsletterForm component (src/components/ui/NewsletterForm.js)
   - Visme B2B Newsletter subscription form integration
   - Async script loading for embeds
   - Optional display mode
   - Professional styling with benefits section

✅ Enhanced Login Page (src/app/login/page.js)
   - Added comprehensive website description
   - Three key differentiators displayed:
     * Multi-role Access Control
     * Real-time Approval System
     * Secure Broadcasting
   - Integrated newsletter form below login
   - Improved user engagement and lead generation

✅ Documentation created:
   - LOGIN_PAGE_ANALYSIS.md - Strategic analysis and KPIs
   - LOGIN_PAGE_IMPLEMENTATION_SUMMARY.md - Technical implementation details

NEW COMPONENT:
- NewsletterForm.js: Embeds Visme form for newsletter signups
  * Handles script lifecycle
  * Responsive design
  * Optional display toggle
  * Includes subscription benefits
  * Styled with gradient buttons and icons

USER FLOW IMPROVEMENT:
Before: User → Role Selection → Login → Dashboard
After: User → Role Selection + Platform Info → Login → Newsletter (Optional) → Dashboard

ENGAGEMENT METRICS TRACKED:
- Newsletter signup rate (target: 20-30%)
- Form completion time
- Role-based signup preferences
- Lead generation data

NEXT STEPS:
1. Configure Visme to send to email service (Mailchimp/HubSpot)
2. Create welcome email sequence
3. Set up weekly newsletter content
4. Monitor analytics and A/B test messaging
5. Add privacy policy link


8. LOGIN PAGE ANIMATION & UX IMPROVEMENTS (v2.1 - May 6, 2026)
-------------------------------------------------------------

ANIMATION ENHANCEMENTS:
✅ Role Selection Buttons:
   - Better hover scale (102%) instead of just icon scaling
   - Click feedback with active:scale-95
   - Icon scales to 110% on hover
   - Longer 300ms transitions for smoothness
   - Arrow fades and slides on hover

✅ Role-Specific Login Header:
   - Shows selected role icon (🎓 Teacher or 🛡️ Principal)
   - Icon has role-specific color gradient
   - Dynamic description shows below heading:
     * Teacher: "Upload content, manage schedules, track approvals"
     * Principal: "Review content, approve/reject, manage broadcasts"
   - Better visual hierarchy and context

✅ Cascading Form Animations:
   - Email field: animationDelay 0.1s
   - Password field: animationDelay 0.2s
   - Submit button: animationDelay 0.3s
   - Demo credentials: animationDelay 0.4s
   - Progressive reveal reduces cognitive load

✅ Form Input Improvements:
   - Focus state shows primary color border
   - Icons transition smoothly on focus
   - Error states clearly marked in red
   - 200ms duration for snappy feedback

✅ Interactive Feedback:
   - Password toggle: hover scale-110, click scale-95
   - Sign In button: hover scale-105, click scale-95
   - Back button: hover translate-x-1 smoothly
   - Demo credentials: hover scale-102

ANIMATION TIMING:
- Button press feedback: 200-300ms
- Icon animations: 300ms for hover, 200ms for interactions
- Form field cascades: 0.1s, 0.2s, 0.3s, 0.4s stagger
- All easing: smooth cubic-bezier (default CSS easing)

USER EXPERIENCE FLOW:
1. User hovers over role → Smooth visual feedback
2. User clicks role → Press effect + form slides in
3. Form elements appear → Sequential fade-in
4. User interacts → Smooth transitions throughout
5. User submits → Loading spinner with feedback

RESPONSIVE DESIGN:
- Mobile (375px): Full width, stacked elements
- Tablet (768px): Proper spacing, side-by-side
- Desktop (1024px+): Max-width 448px, centered

NEW DOCUMENTATION:
- LOGIN_PAGE_ANIMATION_IMPROVEMENTS.md (comprehensive guide)


9. AUTHENTICATION FLOW IMPROVEMENTS (v3.0 - May 6, 2026)
-------------------------------------------------------

ANIMATED REDIRECT FLOW:
✅ Added isRedirecting state to LoginForm component
   - Tracks redirect animation state
   - Shows beautiful redirect screen
   - Prevents multiple redirects

✅ Animated Redirect Screen:
   - Full-screen overlay after successful login
   - Bouncing logo with animate-bounce
   - "Authenticating" heading and "Redirecting to your dashboard..." subtext
   - Animated loading dots with staggered delays (0s, 0.2s, 0.4s)
   - Radial gradient background pulse effect
   - 1-second animation before actual redirect
   - Smooth fade-in entrance animation

✅ Enhanced Submit Button:
   - Text changes: "Sign In" → "Authenticating..."
   - Animated spinner with border rotation
   - Background pulse effect during submission
   - Button disabled during auth and redirect
   - Opacity reduced to 60% when disabled
   - Better visual feedback on state changes

✅ Form Overlay During Submission:
   - Subtle gradient overlay (transparent to black/5)
   - Appears when form is submitting
   - Fade-in animation (300ms)
   - Non-interactive (pointer-events-none)
   - Gives sense of processing

AUTHENTICATION FLOW (COMPLETE):
Role Selection
  ↓ (hover: scale-102, click: scale-95)
Login Form with Role Details
  ↓ (cascading animations 0.1-0.4s)
User Enters Credentials
  ↓ (focus: border color, error: red border)
User Clicks Sign In
  ↓ (click: scale-95, pulse: animate-pulse)
Form Shows Loading Overlay
  ↓ (gradient overlay + disabled button)
Authentication Success
  ↓ (setIsRedirecting = true)
Animated Redirect Screen Appears
  ↓ (bouncing logo, loading dots)
1-Second Animation Complete
  ↓ (animation delay: 1000ms)
Redirect to Dashboard
  ↓ (router.replace)
Teacher → Teacher Dashboard
Principal → Principal Dashboard

REDIRECT SCREEN COMPONENTS:
1. Bouncing Logo
   - Size: w-20 h-20 (80x80px)
   - Animation: animate-bounce (CSS bounce effect)
   - Color: White
   - Gradient background: gradient-bg

2. Heading
   - Text: "Authenticating"
   - Style: text-2xl font-bold gradient-text
   - Color: Primary gradient

3. Subtext
   - Text: "Redirecting to your dashboard..."
   - Style: text-secondary color
   - Size: Regular text

4. Loading Dots
   - Count: 3 dots
   - Size: w-2 h-2 (8x8px)
   - Animation: animate-bounce with staggered delays
   - Color: Primary color
   - Delays: 0s, 0.2s, 0.4s

ANIMATION DURATIONS:
- Redirect screen entrance: 300ms (fade-in)
- Logo bounce: 1000ms (infinite, shows during animation)
- Loading dots: Infinite (staggered bounce)
- Button pulse: Infinite (during submission)
- Form overlay: 300ms (fade-in)
- Total redirect wait: 1000ms (before router.replace)

STATE MANAGEMENT:
- isRedirecting: Tracks animation state
- isSubmitting: Tracks form submission
- Both prevent button clicks during processing
- Cleanup timers on component unmount

TECHNICAL IMPLEMENTATION:
```javascript
const [isRedirecting, setIsRedirecting] = useState(false);

useEffect(() => {
  if (isAuthenticated && user) {
    setIsRedirecting(true);
    
    const timer = setTimeout(() => {
      const path = user.role === ROLES.PRINCIPAL 
        ? ROUTES.PRINCIPAL.DASHBOARD 
        : ROUTES.TEACHER.DASHBOARD;
      router.replace(path);
    }, 1000);

    return () => clearTimeout(timer);
  }
}, [isAuthenticated, user, router]);
```

RESPONSIVE BEHAVIOR:
- Mobile: Full screen animation, properly scaled
- Tablet: Centered with proper spacing
- Desktop: Centered with max-width constraints
- Logo and dots scale appropriately

USER EXPERIENCE:
Before: Click Sign In → Instant redirect (jarring)
After: Click Sign In → Beautiful animation → Smooth redirect (polished)

BENEFITS:
1. Professional appearance with animations
2. Clear feedback that authentication is processing
3. Smooth transition to dashboard
4. No broken loading states
5. Works for both teacher and principal roles
6. Responsive on all device sizes

SECURITY:
- No sensitive data shown during redirect
- Credentials not logged or displayed
- Error handling preserved
- Role-based routing still enforced

PERFORMANCE:
- CSS-based animations (GPU accelerated)
- No JavaScript overhead
- Smooth 60fps animation
- Fast redirect after auth
- Optimized for all devices

FILES MODIFIED:
- src/app/login/page.js
  * Added isRedirecting state
  * Added redirect animation screen
  * Enhanced submit button loading state
  * Improved form overlay

NEW DOCUMENTATION:
- AUTHENTICATION_FLOW_IMPROVEMENTS.md (detailed guide)


10. MERGED NEWSLETTER + ROLE SELECTION WITH ANIMATIONS (v4.0 - May 6, 2026)
---------------------------------------------------------------------------

INTEGRATION IMPROVEMENTS:
✅ Merged Newsletter and Role Selection into one section
   - Newsletter card now appears above role buttons
   - Single cohesive section with smooth cascading animations
   - Removed separate NewsletterForm component
   - Cleaner visual hierarchy

✅ Newsletter Card Design:
   - 📧 Email icon with gradient background
   - "Stay Updated" heading
   - "Subscribe to ContentCast updates" subtext
   - Three animated benefits:
     * Weekly tips & tutorials (fade-in at 0.1s)
     * Feature announcements (fade-in at 0.2s)
     * Exclusive content access (fade-in at 0.3s)
   - Glass-card styling with hover effect
   - Hover: Scale 102%, border transitions to primary color

✅ Animated Divider:
   - "Choose your role" centered between horizontal lines
   - Appears at 0.4s with fade-in animation
   - Professional visual separator

✅ Cascading Animation Timeline:
   - 0s: Newsletter card fades in
   - 0.1-0.3s: Benefits list items cascade
   - 0.4s: Divider with text appears
   - 0.5s: Teacher button fades in
   - 0.6s: Principal button fades in
   - Total animation time: 0.9 seconds

ROLE BUTTON ANIMATIONS:
- Teacher Button: fade-in at 0.5s delay
- Principal Button: fade-in at 0.6s delay
- Both preserve existing hover/click effects:
  * Hover: Scale 102%, icon scales 110%
  * Click: Scale 95%, press feedback
  * Arrow slides in and appears on hover

BENEFITS OF MERGE:
1. Single section instead of separate components
2. No redundant "Choose your role" headings
3. Better user engagement (newsletter benefits visible immediately)
4. Faster initial load (no async component loading)
5. Smoother animations (cascading effect)
6. Cleaner codebase and easier maintenance
7. Better visual flow and hierarchy

CODE STRUCTURE:
- Removed NewsletterForm import
- Combined all elements into one {!selectedRole && ...} block
- Newsletter card animations using animationDelay
- Divider with inline text styling
- Role buttons with staggered fade-in delays
- All within main content area

RESPONSIVE DESIGN:
- Mobile (375px): Full-width, stacked elements, scaled animations
- Tablet (768px): Proper spacing, side-by-side layout
- Desktop (1024px+): Max-width 448px, centered, GPU-accelerated

USER EXPERIENCE FLOW:
1. User opens login page
   └─ Sees beautiful newsletter card with animated benefits
2. Benefits cascade into view (1-3 seconds)
3. Divider appears separating newsletter from role selection
4. Role buttons fade in sequentially
5. User selects role → Form appears
6. User enters credentials → Login form animates in
7. User submits → Redirect animation plays
8. Smooth transition to dashboard

PERFORMANCE:
- CSS-based animations (GPU accelerated)
- No JavaScript overhead
- 60fps smooth animation
- No async loading delays
- Optimized file structure

FILES MODIFIED:
- src/app/login/page.js
  * Removed NewsletterForm import
  * Merged newsletter + role selector
  * Added cascading animations
  * Removed separate newsletter section

NEW DOCUMENTATION:
- NEWSLETTER_ROLE_MERGE_SUMMARY.md (merge details)
